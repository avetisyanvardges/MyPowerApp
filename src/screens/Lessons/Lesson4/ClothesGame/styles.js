import {StyleSheet} from 'react-native';
import {Sizes} from '../../../../assets/RootStyles';

const styles = () => {
  return StyleSheet.create({
    clothes: {
      left: Sizes.size100,
      top: Sizes.size100,
    },
  });
};

export {styles};
